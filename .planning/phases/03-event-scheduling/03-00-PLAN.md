---
phase: 03-event-scheduling
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - shared/src/commonTest/kotlin/event/EventModelTest.kt
  - shared/src/commonTest/kotlin/event/RecurringExpansionTest.kt
  - shared/src/commonTest/kotlin/event/TimezoneDisplayTest.kt
  - server/src/test/kotlin/event/EventCrudTest.kt
  - server/src/test/kotlin/event/RecurringEventTest.kt
  - server/src/test/kotlin/event/EditCancelScopeTest.kt
  - composeApp/src/androidTest/kotlin/event/EventListScreenTest.kt
  - composeApp/src/androidTest/kotlin/event/CalendarViewTest.kt
  - composeApp/src/androidTest/kotlin/event/SubGroupTargetingTest.kt
autonomous: true
requirements: [ES-01, ES-02, ES-03, ES-04, ES-05, ES-06, ES-07, ES-08, ES-09, ES-10, ES-11, ES-12]

must_haves:
  truths:
    - "All 9 Wave 0 test stub files exist with placeholder test methods"
    - "Each stub file compiles and runs (tests are @Ignore or pending)"
    - "Test file package and class names match VALIDATION.md references exactly"
  artifacts:
    - path: "shared/src/commonTest/kotlin/event/EventModelTest.kt"
      provides: "Stub tests for ES-01 through ES-04"
      contains: "class EventModelTest"
    - path: "shared/src/commonTest/kotlin/event/RecurringExpansionTest.kt"
      provides: "Stub tests for ES-08"
      contains: "class RecurringExpansionTest"
    - path: "shared/src/commonTest/kotlin/event/TimezoneDisplayTest.kt"
      provides: "Stub tests for ES-12"
      contains: "class TimezoneDisplayTest"
    - path: "server/src/test/kotlin/event/EventCrudTest.kt"
      provides: "Stub tests for ES-01 through ES-04 server routes"
      contains: "class EventCrudTest"
    - path: "server/src/test/kotlin/event/RecurringEventTest.kt"
      provides: "Stub tests for ES-08 through ES-10"
      contains: "class RecurringEventTest"
    - path: "server/src/test/kotlin/event/EditCancelScopeTest.kt"
      provides: "Stub tests for ES-09 through ES-10"
      contains: "class EditCancelScopeTest"
    - path: "composeApp/src/androidTest/kotlin/event/EventListScreenTest.kt"
      provides: "Stub UI tests for ES-05 through ES-06"
      contains: "class EventListScreenTest"
    - path: "composeApp/src/androidTest/kotlin/event/CalendarViewTest.kt"
      provides: "Stub UI test for ES-07"
      contains: "class CalendarViewTest"
    - path: "composeApp/src/androidTest/kotlin/event/SubGroupTargetingTest.kt"
      provides: "Stub UI test for ES-11"
      contains: "class SubGroupTargetingTest"
  key_links: []
---

<objective>
Create all 9 Wave 0 test stub files declared in VALIDATION.md. Each stub contains a skeleton test class with @Ignore placeholder tests so the test infrastructure exists before any implementation plans execute.

Purpose: Nyquist compliance requires test files to exist before implementation tasks reference them. Wave 0 must complete before Wave 1.
Output: 9 compilable test stub files across shared, server, and composeApp modules.
</objective>

<execution_context>
@/Users/miggi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/miggi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-event-scheduling/03-VALIDATION.md
@.planning/phases/03-event-scheduling/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared module test stubs (EventModelTest, RecurringExpansionTest, TimezoneDisplayTest)</name>
  <files>
    shared/src/commonTest/kotlin/event/EventModelTest.kt
    shared/src/commonTest/kotlin/event/RecurringExpansionTest.kt
    shared/src/commonTest/kotlin/event/TimezoneDisplayTest.kt
  </files>
  <read_first>
    - shared/src/commonTest/
    - shared/build.gradle.kts
  </read_first>
  <action>
Check existing test structure in `shared/src/commonTest/` to determine the test framework (Kotest or kotlin.test). Match the same package/import pattern.

Create `shared/src/commonTest/kotlin/event/EventModelTest.kt`:
```kotlin
package event

import kotlin.test.Test
import kotlin.test.Ignore

class EventModelTest {
    @Test @Ignore("Wave 0 stub - ES-01: implement when Event model exists")
    fun `event has required fields`() {}

    @Test @Ignore("Wave 0 stub - ES-02: implement when Event model exists")
    fun `event type must be training match or other`() {}

    @Test @Ignore("Wave 0 stub - ES-03: implement when cancel logic exists")
    fun `cancelled event has cancelled status and timestamp`() {}

    @Test @Ignore("Wave 0 stub - ES-04: implement when duplicate logic exists")
    fun `duplicated event has new id and active status`() {}
}
```

Create `shared/src/commonTest/kotlin/event/RecurringExpansionTest.kt`:
```kotlin
package event

import kotlin.test.Test
import kotlin.test.Ignore

class RecurringExpansionTest {
    @Test @Ignore("Wave 0 stub - ES-08: implement when materialisation exists")
    fun `weekly pattern expands to correct dates`() {}

    @Test @Ignore("Wave 0 stub - ES-08: implement when materialisation exists")
    fun `daily pattern expands to correct dates`() {}

    @Test @Ignore("Wave 0 stub - ES-08: implement when materialisation exists")
    fun `custom interval pattern expands correctly`() {}

    @Test @Ignore("Wave 0 stub - ES-08: implement when series end date exists")
    fun `series with end date stops expanding after end`() {}
}
```

Create `shared/src/commonTest/kotlin/event/TimezoneDisplayTest.kt`:
```kotlin
package event

import kotlin.test.Test
import kotlin.test.Ignore

class TimezoneDisplayTest {
    @Test @Ignore("Wave 0 stub - ES-12: implement when timezone conversion exists")
    fun `UTC instant converts to local display time`() {}

    @Test @Ignore("Wave 0 stub - ES-12: implement when form exists")
    fun `local form input converts to UTC instant`() {}

    @Test @Ignore("Wave 0 stub - ES-12: implement when timezone label exists")
    fun `timezone abbreviation label is shown on form`() {}
}
```

Adapt the package declaration and imports to match the project's existing test conventions. If tests use Kotest instead of kotlin.test, use the equivalent Kotest annotations (e.g., `xtest` or `!` prefix for ignored tests).
  </action>
  <verify>
    <automated>cd /Users/miggi/miggisrc/teamorg && ls shared/src/commonTest/kotlin/event/*.kt | wc -l</automated>
  </verify>
  <done>3 shared test stub files exist with @Ignore placeholder tests matching VALIDATION.md references</done>
</task>

<task type="auto">
  <name>Task 2: Server test stubs (EventCrudTest, RecurringEventTest, EditCancelScopeTest)</name>
  <files>
    server/src/test/kotlin/event/EventCrudTest.kt
    server/src/test/kotlin/event/RecurringEventTest.kt
    server/src/test/kotlin/event/EditCancelScopeTest.kt
  </files>
  <read_first>
    - server/src/test/kotlin/
    - server/build.gradle.kts
  </read_first>
  <action>
Check existing test structure in `server/src/test/kotlin/` for the test framework and package pattern. Match it.

Create `server/src/test/kotlin/event/EventCrudTest.kt`:
```kotlin
package event

import org.junit.jupiter.api.Test
import org.junit.jupiter.api.Disabled

class EventCrudTest {
    @Test @Disabled("Wave 0 stub - ES-01: create event via API")
    fun `POST events creates one-off event`() {}

    @Test @Disabled("Wave 0 stub - ES-02: event types")
    fun `POST events validates event type`() {}

    @Test @Disabled("Wave 0 stub - ES-03: cancel event")
    fun `POST events cancel sets cancelled status`() {}

    @Test @Disabled("Wave 0 stub - ES-04: duplicate event")
    fun `POST events duplicate creates copy`() {}

    @Test @Disabled("Wave 0 stub - ES-01: get events")
    fun `GET user events returns filtered list`() {}
}
```

Create `server/src/test/kotlin/event/RecurringEventTest.kt`:
```kotlin
package event

import org.junit.jupiter.api.Test
import org.junit.jupiter.api.Disabled

class RecurringEventTest {
    @Test @Disabled("Wave 0 stub - ES-08: create recurring series")
    fun `POST events with recurring creates series and materialises`() {}

    @Test @Disabled("Wave 0 stub - ES-09: edit recurring this only")
    fun `PATCH event with scope this_only updates single event`() {}

    @Test @Disabled("Wave 0 stub - ES-09: edit recurring this and future")
    fun `PATCH event with scope this_and_future splits series`() {}

    @Test @Disabled("Wave 0 stub - ES-10: cancel recurring all")
    fun `POST cancel with scope all cancels entire series`() {}
}
```

Create `server/src/test/kotlin/event/EditCancelScopeTest.kt`:
```kotlin
package event

import org.junit.jupiter.api.Test
import org.junit.jupiter.api.Disabled

class EditCancelScopeTest {
    @Test @Disabled("Wave 0 stub - ES-09: scope this_only preserves overrides")
    fun `edit this_only marks event as series override`() {}

    @Test @Disabled("Wave 0 stub - ES-09: scope this_and_future creates new series")
    fun `edit this_and_future ends old series and creates new`() {}

    @Test @Disabled("Wave 0 stub - ES-10: scope all updates template and future events")
    fun `edit all updates series template and non-override events`() {}

    @Test @Disabled("Wave 0 stub - ES-10: past events unchanged")
    fun `edit all does not modify past events`() {}
}
```

Adapt imports to match the actual test framework used in the server module (JUnit 5 / Kotest). Check existing test files for the pattern.
  </action>
  <verify>
    <automated>cd /Users/miggi/miggisrc/teamorg && ls server/src/test/kotlin/event/*.kt | wc -l</automated>
  </verify>
  <done>3 server test stub files exist with @Disabled placeholder tests matching VALIDATION.md references</done>
</task>

<task type="auto">
  <name>Task 3: Android UI test stubs (EventListScreenTest, CalendarViewTest, SubGroupTargetingTest)</name>
  <files>
    composeApp/src/androidTest/kotlin/event/EventListScreenTest.kt
    composeApp/src/androidTest/kotlin/event/CalendarViewTest.kt
    composeApp/src/androidTest/kotlin/event/SubGroupTargetingTest.kt
  </files>
  <read_first>
    - composeApp/src/androidTest/
    - composeApp/build.gradle.kts
  </read_first>
  <action>
Check if `composeApp/src/androidTest/` exists. If not, create the directory structure. Check existing Android test files for the pattern (Compose UI test with `createComposeRule` or similar).

Create `composeApp/src/androidTest/kotlin/event/EventListScreenTest.kt`:
```kotlin
package event

import org.junit.Test
import org.junit.Ignore

class EventListScreenTest {
    @Test @Ignore("Wave 0 stub - ES-05: event list displays events")
    fun `event list shows upcoming events`() {}

    @Test @Ignore("Wave 0 stub - ES-06: event list filters by team")
    fun `event list filters by selected team`() {}

    @Test @Ignore("Wave 0 stub - ES-06: event list filters by type")
    fun `event list filters by event type`() {}

    @Test @Ignore("Wave 0 stub - ES-05: cancelled event styling")
    fun `cancelled event shows with reduced alpha and chip`() {}
}
```

Create `composeApp/src/androidTest/kotlin/event/CalendarViewTest.kt`:
```kotlin
package event

import org.junit.Test
import org.junit.Ignore

class CalendarViewTest {
    @Test @Ignore("Wave 0 stub - ES-07: calendar month view")
    fun `month view shows colored dots for events`() {}

    @Test @Ignore("Wave 0 stub - ES-07: calendar week view")
    fun `week view shows time blocks for events`() {}

    @Test @Ignore("Wave 0 stub - ES-07: month week toggle")
    fun `month week toggle switches view`() {}
}
```

Create `composeApp/src/androidTest/kotlin/event/SubGroupTargetingTest.kt`:
```kotlin
package event

import org.junit.Test
import org.junit.Ignore

class SubGroupTargetingTest {
    @Test @Ignore("Wave 0 stub - ES-11: subgroup targeting on create")
    fun `create form shows subgroups for selected teams`() {}

    @Test @Ignore("Wave 0 stub - ES-11: subgroup filter on list")
    fun `event targeted to subgroup only visible to subgroup members`() {}
}
```

Adapt imports to match the actual Android test framework used in composeApp (JUnit 4 for androidTest is typical, or Compose UI test if set up).
  </action>
  <verify>
    <automated>cd /Users/miggi/miggisrc/teamorg && ls composeApp/src/androidTest/kotlin/event/*.kt | wc -l</automated>
  </verify>
  <done>3 Android UI test stub files exist with @Ignore placeholder tests matching VALIDATION.md references</done>
</task>

</tasks>

<verification>
- All 9 test stub files exist in the correct directories
- Each file has a class name matching the VALIDATION.md test references
- Files compile (stubs with @Ignore/@Disabled should not fail)
</verification>

<success_criteria>
- 9 test stub files created across shared, server, and composeApp modules
- File paths and class names match VALIDATION.md Wave 0 Requirements exactly
- VALIDATION.md wave_0_complete can be set to true after this plan executes
</success_criteria>

<output>
After completion, create `.planning/phases/03-event-scheduling/03-00-SUMMARY.md`
</output>
