---
phase: 01-foundation-auth
task: iOS simulator crash debugging
status: blocked
last_updated: 2026-03-16T12:35:19.327Z
---

<current_state>
iOS app shows black screen with UIKit lifecycle (SceneDelegate). Previously crashed with MetalRedrawer `check(NSThread.isMainThread)` under SwiftUI lifecycle on iOS 19 (Darwin 25 / macOS 26).
</current_state>

<completed_work>

- Fixed PlistSanityCheck crash: `enforceStrictPlistSanityCheck = false` in `MainViewController.kt`
- Fixed `parallelRendering` compile error: added `@OptIn(ExperimentalComposeUiApi::class)`
- Fixed CMP + Tuist plist: `infoPlist: .extendingDefault(with:)` with correct `UIWindowSceneSessionRoleApplication` in `iosApp/Project.swift`
- Identified MetalRedrawer root cause: `check(NSThread.isMainThread)` at `MetalRedrawer.uikit.kt:342` — SwiftUI render engine calls `ComposeView.drawRect:` on non-main thread on iOS 19
- Switched `iOSApp.swift` to UIKit `AppDelegate` + `SceneDelegate` lifecycle to bypass SwiftUI renderer
- All code fixes already applied: navigationevent pin, circular viewModelScope cast, KGP --no-configuration-cache rebuild

</completed_work>

<remaining_work>

- **Black screen with UIKit lifecycle**: content never renders (or 500ms delay then nothing)
  - CMP's `CMPViewController.scheduleHierarchyContainmentCheck` uses `dispatch_after(500ms)` before calling `viewControllerDidEnterWindowHierarchy` (which creates MetalView)
  - With UIKit root VC, scene is `UISceneActivationStateUnattached` during `willConnectTo`, transitions to active after — `SceneForegroundStateListener` should handle this via `UISceneWillEnterForegroundNotification`
  - Either the notification doesn't fire OR `viewControllerDidEnterWindowHierarchy` never calls because `view.window` is nil at block fire time — UNCONFIRMED

</remaining_work>

<decisions_made>

- UIKit lifecycle chosen over SwiftUI to avoid iOS 19 background-thread `drawRect:` issue
- `enforceStrictPlistSanityCheck = false` + `parallelRendering = false` kept in `MainViewController.kt` as safety net
- `kmpViewModel()` uses `KoinPlatformTools.defaultContext()` (bypasses `AndroidParametersHolder` IrLinkageError) — working
- Kotlin/Native 2.3.10 + iOS 19 may have ObjC bridge threading bug for `NSThread.isMainThread` — unconfirmed

</decisions_made>

<blockers>

- **Black screen**: UIKit lifecycle doesn't render. Two candidate causes:
  1. `viewControllerDidEnterWindowHierarchy` never fires (500ms dispatch block, view.window nil?)
  2. `SceneForegroundStateListener.sceneWillEnterForeground` never fires `isActive = true`
- **MetalRedrawer crash (SwiftUI path)**: `check(NSThread.isMainThread)` fails on iOS 19 — possibly Kotlin 2.3.10 ObjC bridge bug or iOS 19 SwiftUI render threading change

</blockers>

<context>
Two approaches, both broken on iOS 19 simulator. Next session should:
1. Try adding `sceneDidBecomeActive` to SceneDelegate that calls `window?.rootViewController?.view.setNeedsLayout()` + `layoutIfNeeded()` + `setNeedsDisplay()` to force rendering after scene activates
2. OR: Revert to SwiftUI lifecycle and look for a CMP upgrade (check if CMP 1.10.2+ exists and fixes the isMainThread check)
3. OR: Keep SwiftUI but wrap ComposeUIViewController in a Swift UIViewController subclass that overrides `viewWillAppear`/`viewDidAppear` to ensure CMP gets proper lifecycle

Key files:
- `iosApp/iosApp/iOSApp.swift` — currently UIKit SceneDelegate
- `composeApp/src/iosMain/kotlin/com/playbook/MainViewController.kt` — has enforceStrictPlistSanityCheck=false, parallelRendering=false
- CMP source at: `~/.gradle/caches/.../ui-uikitsimarm64/1.10.1/...-sources.jar`
- `MetalRedrawer.uikit.kt:342` = `check(NSThread.isMainThread)`
- `ComposeView.uikit.kt:136-148` = `drawRect` calls `draw(waitUntilCompletion=true)` on first draw
- `CMPViewController` has `scheduleHierarchyContainmentCheck` with 500ms dispatch_after
</context>

<next_action>
In SceneDelegate, add `sceneDidBecomeActive(_:)` that forces layout/display:
```swift
func sceneDidBecomeActive(_ scene: UIScene) {
    window?.rootViewController?.view.setNeedsLayout()
    window?.rootViewController?.view.layoutIfNeeded()
    window?.rootViewController?.view.setNeedsDisplay()
}
```
If still black, add logging to verify `viewWillAppear` is being called on the Compose VC. If not, UIKit root VC lifecycle isn't firing — may need to use `addChild(vc)` pattern from a parent Swift VC.
</next_action>
