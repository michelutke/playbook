---
phase: 04.1-attendance-integration-fixes
verified: 2026-03-24T18:00:00Z
status: human_needed
score: 5/5 must-haves verified
human_verification:
  - test: "Open EventDetailScreen as coach, tap check-in list — verify member names, avatars, and response statuses appear (not empty)"
    expected: "List shows each team member with userName, userAvatar, and their attendance response status"
    why_human: "Requires running app + auth + seeded event data; can't verify deserialization success via grep"
  - test: "Remove a player from a team, then open any event for that team as coach and view check-in list"
    expected: "No crash/NPE; removed member does not appear in list; remaining members show correctly"
    why_human: "Requires DB state manipulation + runtime; NPE safety only verifiable at runtime"
  - test: "Open Profile tab from bottom nav as a player with attendance history"
    expected: "PlayerProfileScreen renders with real stats (non-zero training/match percentages where applicable)"
    why_human: "Requires seeded attendance data + running app; 0% vs real % not distinguishable statically"
  - test: "Open Profile tab from bottom nav as a player with NO team membership yet"
    expected: "Screen renders gracefully (no crash); shows empty/loading state — not a blank crash"
    why_human: "Edge case: getMyRoles() returns empty roles; teamId stays empty string; loadProfile not called"
---

# Phase 4.1: Attendance Integration Fixes — Verification Report

**Phase Goal:** Fix P0/P1 attendance integration gaps — server CheckInEntry shape mismatch, TeamRolesTable nullable alignment, client stats emptyMap fix, Profile tab navigation wiring.
**Verified:** 2026-03-24T18:00:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|---------|
| 1 | GET /events/{id}/check-in returns JSON matching CheckInEntry shape (userId, userName, userAvatar, response, record) | VERIFIED | `CheckInRoutes.kt:25` calls `attendanceRepo.getCheckInEntries(eventId)`; `AttendanceRepository.kt:63-69` defines `CheckInEntryResponse` with exact field names and types matching shared `CheckInEntry` |
| 2 | Client deserializes check-in response into List<CheckInEntry> without error | VERIFIED (automated) / NEEDS HUMAN (runtime) | Server DTO field names match shared `CheckInEntry` exactly; all UUID fields are `String`, all timestamps are `kotlinx.datetime.Instant`; runtime deserialization needs human confirmation |
| 3 | Coach sees populated member response list on EventDetailScreen | NEEDS HUMAN | Code path is correct end-to-end; requires running app with seeded data |
| 4 | Queries on team_roles with NULL user_id do not throw NPE | VERIFIED | `TeamsTable.kt:22` — `uuid("user_id").references(..., onDelete = ReferenceOption.SET_NULL).nullable()`; no bare `row[TeamRolesTable.userId]` reads found anywhere in server codebase; `getCheckInEntries` uses `innerJoin UsersTable` which naturally excludes NULL-userId rows |
| 5 | Member removal preserves attendance data (SET NULL, not CASCADE) | VERIFIED | `TeamsTable.kt:22` uses `ReferenceOption.SET_NULL` (not CASCADE); commit `b9890d5` confirmed |
| 6 | Stats bar shows correct per-type breakdown (training vs match presence %) | VERIFIED (logic) / NEEDS HUMAN (output) | `PlayerProfileViewModel.loadStats` builds real `eventTypes` map from `eventRepository.getMyEvents()`; no `emptyMap()` call; `AttendanceStatsCalculator.calculateStats` receives populated map |
| 7 | Player taps Profile tab and sees PlayerProfileScreen with their attendance stats | VERIFIED (wiring) / NEEDS HUMAN (visual) | `AppNavigation.kt:194-222` — `Screen.Profile` case renders `PlayerProfileScreen`; no `PlaceholderScreen("Profile")` remains |

**Score:** 5/5 truths fully verified at code level; 4 items additionally need human runtime confirmation

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `server/src/main/kotlin/ch/teamorg/routes/CheckInRoutes.kt` | GET /events/{id}/check-in returning joined CheckInEntry-shaped response | VERIFIED | Line 25: `val entries = attendanceRepo.getCheckInEntries(eventId)` — no longer returns `CheckInRow` |
| `server/src/main/kotlin/ch/teamorg/domain/repositories/AttendanceRepository.kt` | CheckInEntryResponse DTO + getCheckInEntries interface method | VERIFIED | Lines 38-69: `@Serializable` DTOs `AttendanceResponseDto`, `AttendanceRecordDto`, `CheckInEntryResponse`; line 77: `getCheckInEntries` in interface |
| `server/src/main/kotlin/ch/teamorg/domain/repositories/AttendanceRepositoryImpl.kt` | Joined query across attendance_responses, check_in_records, users, team_roles | VERIFIED | Lines 71-131: `getCheckInEntries` queries event_teams → team_roles → users (innerJoin), then separate selects for responses + records, merges in-memory |
| `server/src/main/kotlin/ch/teamorg/db/tables/TeamsTable.kt` | TeamRolesTable.userId nullable with SET_NULL | VERIFIED | Line 22: `.references(UsersTable.id, onDelete = ReferenceOption.SET_NULL).nullable()` |
| `composeApp/src/commonMain/kotlin/ch/teamorg/ui/team/PlayerProfileViewModel.kt` | loadStats passes real eventTypes map to calculateStats | VERIFIED | Lines 78-99: builds `eventTypes` from `eventRepository.getMyEvents()`; calls `calculateStats(responses, eventTypes)`; state includes `trainingPresencePct` and `matchPresencePct` |
| `composeApp/src/commonMain/kotlin/ch/teamorg/navigation/AppNavigation.kt` | Screen.Profile routes to PlayerProfileScreen | VERIFIED | Lines 194-222: full `PlayerProfileScreen` setup with `userId` from `UserPreferences`, `teamId` from `getMyRoles()` first role, `LaunchedEffect` calls `viewModel.loadProfile` |
| `composeApp/src/commonMain/kotlin/ch/teamorg/di/UiModule.kt` | PlayerProfileViewModel factory with 5 get() calls | VERIFIED | Line 26: `factory { PlayerProfileViewModel(get(), get(), get(), get(), get()) }` |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `CheckInRoutes.kt` GET handler | `CheckInEntryResponse` DTO shape | `attendanceRepo.getCheckInEntries(eventId)` call | WIRED | Route calls new method; returns `List<CheckInEntryResponse>`; field names match shared `CheckInEntry` contract |
| `TeamsTable.kt` userId column | V8 migration nullable user_id | `.nullable()` + `ReferenceOption.SET_NULL` declaration | WIRED | Column declaration matches migration; no call sites do bare non-null reads of `TeamRolesTable.userId` |
| `PlayerProfileViewModel.loadStats` | `AttendanceStatsCalculator.calculateStats` | `eventTypes` map built from `eventRepository.getMyEvents()` | WIRED | `eventRepository.getMyEvents()` called inside `loadStats`; result mapped to `Map<String, String>`; passed as 2nd arg to `calculateStats` |
| `AppNavigation Screen.Profile` case | `PlayerProfileScreen` composable | `when (screen)` branch | WIRED | `Screen.Profile` branch instantiates `PlayerProfileViewModel` via Koin, resolves `userId`/`teamId`, renders `PlayerProfileScreen`; no `PlaceholderScreen` |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|---------|
| AT-04 | Plan 01 (via BREAK-01) | Player can view attendance list for any event (all team members + statuses) | SATISFIED | `getCheckInEntries` returns all team members with response+record; route serves joined shape |
| AT-11 | Plan 01 (via BREAK-01) | Coach sees all team members + current response for an event | SATISFIED | Same fix — shape now contains `userName`, `userAvatar`, nested `response` object |
| AT-12 | Plan 01 (via BREAK-01) | Coach can manually override attendance status per player | SATISFIED | Depends on AT-11 list loading — wiring now correct; PUT route unchanged and functional |
| AT-13 | Plan 01 (via PARTIAL-01) | Coach can add a note to an attendance entry | SATISFIED | NPE risk eliminated; `AttendanceRecordDto.note` field present |
| TM-19 | Plan 01 (via PARTIAL-01) | Removing a member preserves their historical attendance data | SATISFIED | `SET_NULL` (not CASCADE) on `team_roles.user_id`; attendance rows reference event/user UUIDs directly |
| AT-05 | Plan 02 (via PARTIAL-02) | Player can view attendance statistics (% presence, filterable by event type) | SATISFIED | `loadStats` now populates real `eventTypes`; `trainingPresencePct` and `matchPresencePct` computed from real data |
| AT-06 | Plan 02 (via PARTIAL-03) | Player can declare a recurring weekly absence | SATISFIED | `PlayerProfileScreen` now reachable via Profile tab nav; `AbwesenheitRepository` already wired in `PlayerProfileViewModel` |
| AT-08 | Plan 02 (via PARTIAL-03) | Player can view, edit, delete active Abwesenheit entries | SATISFIED | Same as AT-06 — screen now reachable; absence CRUD wired in ViewModel |
| TM-17 | Plan 02 (via PARTIAL-02, PARTIAL-03) | Player profile shows attendance statistics summary | SATISFIED | Stats computed correctly; profile screen reachable from bottom nav |

**No orphaned requirements** — all 9 IDs from plan frontmatter (`AT-04, AT-11, AT-12, AT-13, TM-19, AT-05, AT-06, AT-08, TM-17`) are accounted for in REQUIREMENTS.md and have implementation evidence.

### Gap Closure Verification

All 4 milestone audit gap IDs are closed:

| Gap ID | Severity | Description | Closed By | Evidence |
|--------|----------|-------------|-----------|---------|
| BREAK-01 | P0 | CheckInEntry shape mismatch | Plan 01 Task 1 / commit 753cd35 | `CheckInRoutes.kt` calls `getCheckInEntries`; DTOs match shared contract |
| PARTIAL-01 | P0 | TeamRolesTable userId not nullable | Plan 01 Task 2 / commit b9890d5 | `TeamsTable.kt:22` — `.nullable()` + `SET_NULL` |
| PARTIAL-02 | P1 | calculateStats called with emptyMap() | Plan 02 Task 1 / commit 052f63e | `PlayerProfileViewModel.loadStats` builds real `eventTypes` map |
| PARTIAL-03 | P1 | Profile tab routes to PlaceholderScreen | Plan 02 Task 2 / commit 3aaf86a | `AppNavigation.kt Screen.Profile` renders `PlayerProfileScreen` |

### Anti-Patterns Found

No anti-patterns found in modified files.

- No `TODO`/`FIXME`/`PLACEHOLDER` comments in changed files
- No stub return values (`return null`, `return {}`, `return []`)
- No `emptyMap()` in `PlayerProfileViewModel.kt`
- No `PlaceholderScreen("Profile")` in `AppNavigation.kt`
- All Kotlin compilation verified via commits (gradlew checks documented in SUMMARYs)

### Human Verification Required

#### 1. Coach check-in list populated

**Test:** Log in as coach, open an event with team members, navigate to check-in/member-response view.
**Expected:** List shows each team member's name, avatar, and their current attendance response status (not empty).
**Why human:** Requires running app + JWT auth + seeded event + team members; deserialization success only verifiable at runtime.

#### 2. Member removal NPE safety

**Test:** Remove a player from a team via admin/coach UI. Then open any event belonging to that team as coach and view the check-in list.
**Expected:** No crash; removed member absent from list; remaining members display correctly.
**Why human:** Requires DB state mutation + runtime; NULL user_id row handling only verifiable at runtime.

#### 3. Profile tab stats accuracy

**Test:** Log in as a player with existing attendance history across training and match events. Open Profile tab from bottom nav.
**Expected:** Stats bar shows non-zero `trainingPresencePct` and/or `matchPresencePct` matching actual attendance record.
**Why human:** Requires seeded data where percentages would be non-zero; static analysis can't distinguish correct 0% from always-0% bug.

#### 4. Profile tab with no team membership

**Test:** Log in as a user with no team roles (e.g., freshly registered). Open Profile tab.
**Expected:** Screen renders gracefully — loading state or empty state — no crash.
**Why human:** `getMyRoles()` returns empty; `teamId` stays `""`; `loadProfile` is not called; edge case behavior only verifiable at runtime.

### Gaps Summary

No gaps. All 5 must-haves verified at code level. 4 items flagged for human runtime verification (normal for UI + data-dependent behavior).

---

_Verified: 2026-03-24T18:00:00Z_
_Verifier: Claude (gsd-verifier)_
