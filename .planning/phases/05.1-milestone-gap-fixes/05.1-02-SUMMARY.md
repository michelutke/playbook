---
phase: 05.1-milestone-gap-fixes
plan: "02"
subsystem: notifications/ui
tags: [team-names, dead-code, koin, viewmodel]
dependency_graph:
  requires: []
  provides: [NO-08-fix, clean-calendar-dead-code]
  affects: [NotificationSettingsViewModel, UiModule, AppNavigation]
tech_stack:
  added: []
  patterns: [ClubRepository-injection, team-name-resolution-map]
key_files:
  created: []
  modified:
    - composeApp/src/commonMain/kotlin/ch/teamorg/ui/inbox/NotificationSettingsViewModel.kt
    - composeApp/src/commonMain/kotlin/ch/teamorg/di/UiModule.kt
    - composeApp/src/commonMain/kotlin/ch/teamorg/navigation/AppNavigation.kt
  deleted:
    - composeApp/src/commonMain/kotlin/ch/teamorg/ui/calendar/CalendarScreen.kt
    - composeApp/src/commonMain/kotlin/ch/teamorg/ui/calendar/CalendarViewModel.kt
decisions:
  - "ClubRepository injected as 3rd param to NotificationSettingsViewModel; team names fetched via getClubTeams per distinct clubId"
  - "Deleted CalendarScreen.kt and CalendarViewModel.kt; Screen.Calendar case retained in AppNavigation using EventListScreen+EventViewMode.CALENDAR"
metrics:
  duration: ~5m
  completed: 2026-04-01
---

# Phase 05.1 Plan 02: Team Name Resolution and Dead Calendar Code Removal Summary

**One-liner:** Inject ClubRepository into NotificationSettingsViewModel to display real team names; delete orphaned CalendarScreen/CalendarViewModel and remove their Koin/import references.

## Tasks Completed

| # | Task | Commit | Files |
|---|------|--------|-------|
| 1 | Resolve team names in NotificationSettingsViewModel | b888062 | NotificationSettingsViewModel.kt, UiModule.kt |
| 2 | Remove dead CalendarScreen and CalendarViewModel | 6ec8c69 | CalendarScreen.kt (deleted), CalendarViewModel.kt (deleted), UiModule.kt, AppNavigation.kt |

## Changes Made

### Task 1 — Team Name Resolution

- Added `ClubRepository` import and 3rd constructor parameter to `NotificationSettingsViewModel`
- `loadTeams()` now collects distinct `clubId`s from `teamRoles`, calls `clubRepository.getClubTeams(clubId)` for each, builds a `teamNameMap`, and maps `teamNameMap[role.teamId] ?: role.teamId` as `teamName`
- Updated Koin factory in `UiModule.kt`: `NotificationSettingsViewModel(get(), get(), get())`

### Task 2 — Dead Code Removal

- Deleted `CalendarScreen.kt` and `CalendarViewModel.kt` (327 + 82 lines of dead UI code)
- Removed `import ch.teamorg.ui.calendar.CalendarViewModel` from `UiModule.kt`
- Removed `factory { CalendarViewModel(get(), get(), get()) }` from `UiModule.kt`
- Removed `CalendarScreen` and `CalendarViewModel` imports from `AppNavigation.kt`
- `Screen.Calendar ->` branch in `AppNavigation` retained — it uses `EventListScreen` + `EventViewMode.CALENDAR`, which is the active implementation

## Verification

- `grep -rn "CalendarViewModel|CalendarScreen" composeApp/src/commonMain/kotlin/` returns no matches
- `grep -n "getClubTeams" NotificationSettingsViewModel.kt` shows team name resolution on line 51
- `./gradlew :composeApp:compileDebugSources --no-daemon` — BUILD SUCCESSFUL

## Deviations from Plan

None - plan executed exactly as written.

## Self-Check: PASSED

- NotificationSettingsViewModel.kt exists and contains `getClubTeams` call
- UiModule.kt contains `NotificationSettingsViewModel(get(), get(), get())`
- CalendarScreen.kt and CalendarViewModel.kt do not exist
- AppNavigation.kt does not contain CalendarScreen/CalendarViewModel imports
- AppNavigation.kt contains `Screen.Calendar ->` case
- Commits b888062 and 6ec8c69 exist
