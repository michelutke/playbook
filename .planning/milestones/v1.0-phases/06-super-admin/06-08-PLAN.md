---
phase: 06-super-admin
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - admin/src/routes/admin/dashboard/+page.server.ts
  - admin/src/routes/admin/dashboard/+page.svelte
  - admin/src/routes/admin/clubs/+page.server.ts
  - admin/src/routes/admin/clubs/+page.svelte
  - admin/src/routes/admin/clubs/[clubId]/+page.server.ts
  - admin/src/routes/admin/clubs/[clubId]/+page.svelte
autonomous: true
gap_closure: true
requirements:
  - SA-03
  - SA-06
  - SA-07
  - SA-09

must_haves:
  truths:
    - "Dashboard 4th stat widget shows a number (recentSignups.length), not undefined"
    - "Dashboard recent sign-ups table renders rows from recentSignups array"
    - "Clubs list pagination shows correct totalPages computed from totalCount"
    - "Club detail Impersonate button passes real userId to form action"
    - "Club detail Remove button passes real userId to form action"
  artifacts:
    - path: "admin/src/routes/admin/dashboard/+page.server.ts"
      provides: "AdminStats interface matching DashboardStats backend shape"
      contains: "recentSignups"
    - path: "admin/src/routes/admin/dashboard/+page.svelte"
      provides: "Dashboard rendering from correct field names"
      contains: "data.stats.recentSignups"
    - path: "admin/src/routes/admin/clubs/+page.server.ts"
      provides: "ClubsResponse interface with totalCount field"
      contains: "totalCount"
    - path: "admin/src/routes/admin/clubs/+page.svelte"
      provides: "Pagination using totalCount"
      contains: "data.clubs.totalCount"
    - path: "admin/src/routes/admin/clubs/[clubId]/+page.server.ts"
      provides: "Manager interface with userId field"
      contains: "userId: string"
    - path: "admin/src/routes/admin/clubs/[clubId]/+page.svelte"
      provides: "Manager actions using userId"
      contains: "manager.userId"
  key_links:
    - from: "admin/src/routes/admin/dashboard/+page.server.ts"
      to: "server AdminRepository.DashboardStats"
      via: "AdminStats TypeScript interface"
      pattern: "recentSignups.*RecentSignup"
    - from: "admin/src/routes/admin/clubs/+page.server.ts"
      to: "server AdminRepository.ClubListPage"
      via: "ClubsResponse TypeScript interface"
      pattern: "totalCount.*number"
    - from: "admin/src/routes/admin/clubs/[clubId]/+page.server.ts"
      to: "server AdminRepository.ClubManagerInfo"
      via: "Manager TypeScript interface"
      pattern: "userId.*string"
---

<objective>
Fix 3 runtime data shape mismatches between SvelteKit frontend TypeScript interfaces and Ktor backend Kotlin @Serializable DTOs.

Purpose: Close verification gaps from 06-VERIFICATION.md — dashboard stats, clubs pagination, and club manager actions are broken because frontend field names don't match backend JSON.
Output: All 6 admin frontend files corrected to match actual backend response shapes.
</objective>

<execution_context>
@/Users/miggi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/miggi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-super-admin/06-VERIFICATION.md

<interfaces>
<!-- Backend Kotlin @Serializable data classes — these are the SOURCE OF TRUTH for JSON field names -->

From server/src/main/kotlin/ch/teamorg/domain/repositories/AdminRepository.kt:

```kotlin
@Serializable
data class DashboardStats(
    val totalClubs: Long,
    val totalUsers: Long,
    val activeEventsToday: Long,
    val recentSignups: List<RecentSignup>   // NOT a number; NOT "recentSignUps"
)

@Serializable
data class RecentSignup(
    val userId: String,
    val email: String,
    val displayName: String,
    val createdAt: String                   // NOT "joinedAt"
)

@Serializable
data class ClubListPage(
    val clubs: List<ClubListItem>,
    val page: Int,
    val pageSize: Int,
    val totalCount: Long                    // NOT "total"
)

@Serializable
data class ClubManagerInfo(
    val userId: String,                     // NOT "id"
    val email: String,
    val displayName: String
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix dashboard stats interface and rendering</name>
  <files>admin/src/routes/admin/dashboard/+page.server.ts, admin/src/routes/admin/dashboard/+page.svelte</files>
  <read_first>
    - admin/src/routes/admin/dashboard/+page.server.ts
    - admin/src/routes/admin/dashboard/+page.svelte
    - server/src/main/kotlin/ch/teamorg/domain/repositories/AdminRepository.kt (lines 27-40, DashboardStats + RecentSignup)
  </read_first>
  <action>
**+page.server.ts** — Replace the entire `AdminStats` interface (lines 4-15) with:

```typescript
interface RecentSignup {
	userId: string;
	email: string;
	displayName: string;
	createdAt: string;
}

interface AdminStats {
	totalClubs: number;
	totalUsers: number;
	activeEventsToday: number;
	recentSignups: RecentSignup[];
}
```

Key changes: remove `recentSignUps: number` and `recentUsers: Array<...>`. Replace with `recentSignups: RecentSignup[]` matching backend field name exactly (lowercase 'u' in Signups).

**+page.svelte** — Make these changes:

1. In the `statCards` array (line 29), change `data.stats.recentSignUps` to `data.stats.recentSignups.length` — this displays the count of recent signups in the 4th widget.

2. In the recent sign-ups table condition (line 78), change `data.stats.recentUsers && data.stats.recentUsers.length > 0` to `data.stats.recentSignups && data.stats.recentSignups.length > 0`.

3. In the table each loop (line 100), change `data.stats.recentUsers` to `data.stats.recentSignups`.

4. In the table row cells (line 109), change `user.joinedAt` to `user.createdAt` (the backend field is `createdAt`, not `joinedAt`).
  </action>
  <verify>
    <automated>cd /Users/miggi/miggisrc/teamorg/admin && grep -c "recentSignUps" src/routes/admin/dashboard/+page.server.ts src/routes/admin/dashboard/+page.svelte; grep -c "recentUsers" src/routes/admin/dashboard/+page.svelte; grep -c "joinedAt" src/routes/admin/dashboard/+page.svelte; echo "--- expect all 0 above ---"; grep -c "recentSignups" src/routes/admin/dashboard/+page.server.ts src/routes/admin/dashboard/+page.svelte; echo "--- expect counts > 0 above ---"</automated>
  </verify>
  <acceptance_criteria>
    - grep "recentSignUps" in both dashboard files returns 0 matches
    - grep "recentUsers" in +page.svelte returns 0 matches
    - grep "joinedAt" in +page.svelte returns 0 matches
    - grep "recentSignups" in +page.server.ts returns at least 1 match
    - grep "recentSignups" in +page.svelte returns at least 3 matches (statCards + table condition + each loop)
    - grep "recentSignups.length" in +page.svelte returns at least 1 match (the stat card count)
    - grep "user.createdAt" in +page.svelte returns at least 1 match
    - npm run check in admin/ passes with no type errors
  </acceptance_criteria>
  <done>Dashboard AdminStats interface matches backend DashboardStats exactly. 4th stat widget renders recentSignups.length as count. Recent sign-ups table iterates recentSignups array with correct field names (createdAt not joinedAt).</done>
</task>

<task type="auto">
  <name>Task 2: Fix clubs pagination and manager ID field mismatches</name>
  <files>admin/src/routes/admin/clubs/+page.server.ts, admin/src/routes/admin/clubs/+page.svelte, admin/src/routes/admin/clubs/[clubId]/+page.server.ts, admin/src/routes/admin/clubs/[clubId]/+page.svelte</files>
  <read_first>
    - admin/src/routes/admin/clubs/+page.server.ts
    - admin/src/routes/admin/clubs/+page.svelte
    - admin/src/routes/admin/clubs/[clubId]/+page.server.ts
    - admin/src/routes/admin/clubs/[clubId]/+page.svelte
    - server/src/main/kotlin/ch/teamorg/domain/repositories/AdminRepository.kt (lines 81-107, ClubListPage + ClubManagerInfo)
  </read_first>
  <action>
**clubs/+page.server.ts** — In the `ClubsResponse` interface (line 16-21), change `total: number` to `totalCount: number`. The full interface becomes:

```typescript
interface ClubsResponse {
	clubs: ClubListItem[];
	page: number;
	pageSize: number;
	totalCount: number;
}
```

**clubs/+page.svelte** — On line 13, change `data.clubs.total` to `data.clubs.totalCount`. The line becomes:

```typescript
let totalPages = $derived(
    data.clubs ? Math.ceil(data.clubs.totalCount / data.clubs.pageSize) : 1
);
```

**clubs/[clubId]/+page.server.ts** — In the `Manager` interface (lines 5-9), change `id: string` to `userId: string`. The full interface becomes:

```typescript
interface Manager {
	userId: string;
	displayName: string;
	email: string;
}
```

**clubs/[clubId]/+page.svelte** — Make these 6 changes:

1. Line 18: Change `$state<{ id: string; name: string } | null>(null)` to `$state<{ userId: string; name: string } | null>(null)` for `removeManagerTarget`.

2. Line 19: Change `$state<{ id: string; name: string } | null>(null)` to `$state<{ userId: string; name: string } | null>(null)` for `impersonateTarget`.

3. Line 210: Change `manager.id` to `manager.userId` — becomes `impersonateTarget = { userId: manager.userId, name: manager.displayName }`.

4. Line 216: Change `manager.id` to `manager.userId` — becomes `removeManagerTarget = { userId: manager.userId, name: manager.displayName }`.

5. Line 408: Change `removeManagerTarget.id` to `removeManagerTarget.userId` — the hidden form input value becomes `value={removeManagerTarget.userId}`.

6. Line 440: Change `impersonateTarget.id` to `impersonateTarget.userId` — the hidden form input value becomes `value={impersonateTarget.userId}`.
  </action>
  <verify>
    <automated>cd /Users/miggi/miggisrc/teamorg/admin && grep -c "total:" src/routes/admin/clubs/+page.server.ts; grep -c "\.total " src/routes/admin/clubs/+page.svelte; echo "--- expect 0 above ---"; grep -c "totalCount" src/routes/admin/clubs/+page.server.ts src/routes/admin/clubs/+page.svelte; echo "--- expect counts > 0 above ---"; grep -c "manager\.id\b" src/routes/admin/clubs/\[clubId\]/+page.svelte; grep -n "id: string" src/routes/admin/clubs/\[clubId\]/+page.server.ts; echo "--- expect 0 matches above ---"; grep -c "manager\.userId" src/routes/admin/clubs/\[clubId\]/+page.svelte; grep -c "userId: string" src/routes/admin/clubs/\[clubId\]/+page.server.ts; echo "--- expect counts > 0 above ---"</automated>
  </verify>
  <acceptance_criteria>
    - grep "total:" in clubs/+page.server.ts returns 0 matches (old field gone)
    - grep "totalCount" in clubs/+page.server.ts returns at least 1 match
    - grep "data.clubs.totalCount" in clubs/+page.svelte returns at least 1 match
    - grep "data.clubs.total " (with trailing space) in clubs/+page.svelte returns 0 matches
    - grep for word-boundary "manager\.id" in [clubId]/+page.svelte returns 0 matches
    - grep "userId: string" in [clubId]/+page.server.ts returns at least 1 match (the Manager interface)
    - grep "manager.userId" in [clubId]/+page.svelte returns at least 2 matches (lines 210, 216)
    - grep "removeManagerTarget.userId" in [clubId]/+page.svelte returns at least 1 match
    - grep "impersonateTarget.userId" in [clubId]/+page.svelte returns at least 1 match
    - npm run check in admin/ passes with no type errors
  </acceptance_criteria>
  <done>Clubs list pagination uses totalCount (matching ClubListPage backend). Club detail manager actions use userId (matching ClubManagerInfo backend). Impersonate and Remove buttons pass correct userId values through form hidden inputs.</done>
</task>

<task type="auto">
  <name>Task 3: Run type check to confirm all fixes compile</name>
  <files></files>
  <read_first>
    - admin/package.json
  </read_first>
  <action>
Run `npm run check` (or `npx svelte-check`) in the admin/ directory to verify all TypeScript interfaces are consistent and no type errors remain after the interface changes. If any errors surface, fix them — they will be caused by leftover references to the old field names.
  </action>
  <verify>
    <automated>cd /Users/miggi/miggisrc/teamorg/admin && npm run check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - npm run check exits with code 0
    - Output contains "0 errors" or equivalent svelte-check success message
    - No TypeScript errors referencing recentSignUps, recentUsers, joinedAt, .total, or manager.id
  </acceptance_criteria>
  <done>All admin frontend TypeScript compiles cleanly. No type errors from data shape mismatches.</done>
</task>

</tasks>

<verification>
All three verification gaps from 06-VERIFICATION.md resolved:

1. **Gap 1 (Dashboard SA-09):** AdminStats.recentSignups matches DashboardStats.recentSignups. 4th widget shows count. Table shows signup rows with correct createdAt field.
2. **Gap 2 (Clubs pagination SA-03):** ClubsResponse.totalCount matches ClubListPage.totalCount. Pagination computes correct totalPages.
3. **Gap 3 (Manager ID SA-06, SA-07):** Manager.userId matches ClubManagerInfo.userId. Impersonate and Remove actions pass real user IDs.
</verification>

<success_criteria>
- Zero grep matches for old field names (recentSignUps, recentUsers, joinedAt, .total as standalone, manager.id)
- Positive grep matches for correct field names (recentSignups, createdAt, totalCount, manager.userId)
- npm run check passes in admin/ directory
- All 6 modified files contain interfaces that exactly match their backend Kotlin @Serializable counterparts
</success_criteria>

<output>
After completion, create `.planning/phases/06-super-admin/06-08-SUMMARY.md`
</output>
