---
phase: 05.2-auth-retroactive-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-foundation-auth/01-VERIFICATION.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05, AUTH-06, TM-14]

must_haves:
  truths:
    - "All 6 AUTH requirements have verification evidence in 01-VERIFICATION.md"
    - "TM-14 multi-team backend support confirmed with evidence"
    - "REQUIREMENTS.md checkboxes checked for AUTH-01 through AUTH-06 and TM-14"
  artifacts:
    - path: ".planning/phases/01-foundation-auth/01-VERIFICATION.md"
      provides: "Verification evidence for AUTH-01 through AUTH-06"
      contains: "AUTH-01"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated checkboxes"
      contains: "[x] **AUTH-01**"
  key_links:
    - from: ".planning/phases/01-foundation-auth/01-VERIFICATION.md"
      to: ".planning/REQUIREMENTS.md"
      via: "Verification evidence justifies checkbox updates"
      pattern: "AUTH-0[1-6]"
---

<objective>
Retroactively verify Phase 01 auth requirements (AUTH-01 through AUTH-06) and close TM-14 partial gap by creating verification documentation and updating requirement checkboxes.

Purpose: Phase 01 was completed before the GSD verification workflow existed. The audit found all AUTH routes ARE wired and functional but lack formal verification artifacts. This plan creates that documentation.
Output: 01-VERIFICATION.md with evidence for all 6 AUTH requirements, REQUIREMENTS.md with 7 newly checked boxes.
</objective>

<execution_context>
@/Users/miggi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/miggi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/05.2-auth-retroactive-verification/05.2-CONTEXT.md
@.planning/phases/02-team-management/02-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 01-VERIFICATION.md with evidence for AUTH-01 through AUTH-06</name>
  <files>.planning/phases/01-foundation-auth/01-VERIFICATION.md</files>
  <read_first>
    - server/src/main/kotlin/ch/teamorg/routes/AuthRoutes.kt
    - server/src/test/kotlin/ch/teamorg/routes/AuthRoutesTest.kt
    - server/src/main/kotlin/ch/teamorg/plugins/Security.kt
    - server/src/main/kotlin/ch/teamorg/domain/repositories/UserRepositoryImpl.kt
    - server/src/main/kotlin/ch/teamorg/routes/InviteRoutes.kt
    - .planning/phases/02-team-management/02-VERIFICATION.md (format reference)
    - .planning/v1.0-MILESTONE-AUDIT.md (gap definitions)
  </read_first>
  <action>
Create `.planning/phases/01-foundation-auth/01-VERIFICATION.md` following the format established in 02-VERIFICATION.md. Include YAML frontmatter with phase, verified date, status, and score fields.

For each AUTH requirement, inspect the actual source code and tests to gather evidence:

**AUTH-01 (Register with email + password):**
- Find POST /register route in AuthRoutes.kt — document the handler, request shape (email, password, name), password hashing (bcrypt), user creation
- Find register test in AuthRoutesTest.kt — document test names proving registration works
- Evidence: route handler + passing test

**AUTH-02 (Login + stay logged in via JWT):**
- Find POST /login route in AuthRoutes.kt — document JWT issuance, token content (userId, roles), expiry
- Find Security.kt JWT configuration — document validation, audience, issuer
- Find login test in AuthRoutesTest.kt
- Evidence: route handler + JWT config + passing test

**AUTH-03 (Logout; session invalidated):**
- Find POST /logout route in AuthRoutes.kt — document how logout works (client-side token discard or server-side invalidation)
- Find logout test in AuthRoutesTest.kt
- Evidence: route handler + test

**AUTH-04 (Email invite prompts registration):**
- Find invite acceptance flow in InviteRoutes.kt — document how accepting an invite for an unregistered email prompts registration
- Find invite test in InviteRoutesTest.kt or AuthRoutesTest.kt
- Evidence: invite route logic + test

**AUTH-05 (Role changes take effect immediately):**
- Find role loading in Security.kt or AuthRoutes.kt — document that roles are loaded from DB on each request (not cached in JWT)
- Look for GET /me/roles endpoint that fetches fresh roles
- Evidence: per-request DB role lookup

**AUTH-06 (SuperAdmin manual only):**
- Confirm no registration endpoint grants SuperAdmin role
- Check that SuperAdmin is only assignable via direct DB manipulation
- Evidence: absence of SuperAdmin in registration flow + DB-only grant pattern

Format each requirement as a row in a table with: REQ-ID, Description, Status (VERIFIED), Evidence summary. Then include detailed evidence sections below the table with file paths and relevant code references.
  </action>
  <verify>
    <automated>grep -c "AUTH-0[1-6]" .planning/phases/01-foundation-auth/01-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at .planning/phases/01-foundation-auth/01-VERIFICATION.md
    - Contains YAML frontmatter with "phase: 01-foundation-auth" and "status:" field
    - Contains all 6 requirement IDs: AUTH-01, AUTH-02, AUTH-03, AUTH-04, AUTH-05, AUTH-06
    - Each requirement has a VERIFIED status with evidence referencing actual file paths
    - grep "AUTH-01" returns at least one match
    - grep "AUTH-06" returns at least one match
    - grep "VERIFIED" returns at least 6 matches
  </acceptance_criteria>
  <done>01-VERIFICATION.md exists with evidence for all 6 AUTH requirements, each marked VERIFIED with source file references and test evidence</done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md checkboxes for AUTH-01 through AUTH-06 and TM-14</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/01-foundation-auth/01-VERIFICATION.md (just created in Task 1)
    - .planning/phases/02-team-management/02-VERIFICATION.md (TM-14 evidence)
  </read_first>
  <action>
Update `.planning/REQUIREMENTS.md` to check 7 boxes:

1. Change `- [ ] **AUTH-01**:` to `- [x] **AUTH-01**:` (line ~6)
2. Change `- [ ] **AUTH-02**:` to `- [x] **AUTH-02**:` (line ~7)
3. Change `- [ ] **AUTH-03**:` to `- [x] **AUTH-03**:` (line ~8)
4. Change `- [ ] **AUTH-04**:` to `- [x] **AUTH-04**:` (line ~9)
5. Change `- [ ] **AUTH-05**:` to `- [x] **AUTH-05**:` (line ~10)
6. Change `- [ ] **AUTH-06**:` to `- [x] **AUTH-06**:` (line ~11)
7. Change `- [ ] **TM-14**:` to `- [x] **TM-14**:` (line ~28)

TM-14 justification: Backend supports multiple TeamRoles rows per user (confirmed in 02-VERIFICATION.md truth #15 as PARTIAL with "no UI gap required"). The backend data model allows a player to belong to multiple teams via multiple team_roles entries. No UI change needed — the app already renders all teams from getMyRoles().

Do NOT change any other checkboxes. Do NOT modify any other content.
  </action>
  <verify>
    <automated>grep -c "\[x\] \*\*AUTH-0[1-6]\*\*" .planning/REQUIREMENTS.md && grep -c "\[x\] \*\*TM-14\*\*" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "[x] **AUTH-01**" returns 1 match
    - grep "[x] **AUTH-02**" returns 1 match
    - grep "[x] **AUTH-03**" returns 1 match
    - grep "[x] **AUTH-04**" returns 1 match
    - grep "[x] **AUTH-05**" returns 1 match
    - grep "[x] **AUTH-06**" returns 1 match
    - grep "[x] **TM-14**" returns 1 match
    - No other checkboxes changed (grep "[ ]" count matches original minus 7)
  </acceptance_criteria>
  <done>All 7 checkboxes updated in REQUIREMENTS.md — AUTH-01 through AUTH-06 and TM-14 now checked</done>
</task>

</tasks>

<verification>
1. `01-VERIFICATION.md` exists and contains evidence for all 6 AUTH requirements
2. `REQUIREMENTS.md` has 7 newly checked boxes (AUTH-01-06 + TM-14)
3. No unchecked AUTH requirements remain: `grep "\[ \] \*\*AUTH" .planning/REQUIREMENTS.md` returns 0 matches
4. TM-14 checked: `grep "\[x\] \*\*TM-14\*\*" .planning/REQUIREMENTS.md` returns 1 match
</verification>

<success_criteria>
- All 6 AUTH requirements have formal verification evidence in 01-VERIFICATION.md
- TM-14 checkbox reflects actual implementation status (backend multi-team confirmed)
- REQUIREMENTS.md shows 62/62 v1 requirements satisfied (excluding SA-01-12 which are Phase 6)
- Re-audit of AUTH section would pass with 6/6 requirements verified
</success_criteria>

<output>
After completion, create `.planning/phases/05.2-auth-retroactive-verification/05.2-01-SUMMARY.md`
</output>
