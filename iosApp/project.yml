name: iosApp
options:
  bundleIdPrefix: com.playbook
targets:
  iosApp:
    type: application
    platform: iOS
    deploymentTarget: "16.0"
    sources:
      - iosApp
    settings:
      PRODUCT_BUNDLE_IDENTIFIER: com.playbook.ios
      MARKETING_VERSION: 1.0
      CURRENT_PROJECT_VERSION: 1
